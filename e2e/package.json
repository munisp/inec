{
  "name": "inec-e2e",
  "private": true,
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:api": "playwright test --project=chromium tests/auth.spec.ts tests/elections.spec.ts tests/observer.spec.ts"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.1"
  }
}
